import React, { useState } from 'react';
import { useDispatch, useSelector } from "react-redux";
import { useHistory } from 'react-router-dom';
import { editEvent } from '../../store/event';

function Event() {

    return (
        <h1>PAGE RENDERS EEEEEE</h1>
    )
}

export default Event;